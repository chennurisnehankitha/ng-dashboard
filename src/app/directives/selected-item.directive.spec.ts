import { SelectedItemDirective } from './selected-item.directive';

describe('SelectedItemDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedItemDirective();
    expect(directive).toBeTruthy();
  });
});
